<div class="container">
  <div class="row mt-3">
    <div class="col-lg-12">
      <h4 class="mt-5">Mes réservations</h4>


        <% @reservations.each do |reservation| %>
        <div class="card-resa">
          <div class="d-flex">

              <%= link_to activites_path(@activite) do %>
                <div class="mr-1"><%= reservation.activite.nom %></div>
              <% end %>


            <div>- <%= reservation.activite.entreprise.nom %></div>
          </div>
          <div><%= reservation.activite.description %></div>
          <div><%= reservation.activite.address %></div>
          <div>Du <%= reservation.date_debut %> Au <%= reservation.date_fin %></div>
          <div><%= reservation.heure_debut %></div>
          <div><%= reservation.heure_fin %></div>
        </div>
      <% end %>

    </div>
  </div>
</div>
